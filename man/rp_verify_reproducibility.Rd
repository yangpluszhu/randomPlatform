% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/output_artifacts.R
\name{rp_verify_reproducibility}
\alias{rp_verify_reproducibility}
\title{Verify a reproduced randomization table against an original table or hash.}
\usage{
rp_verify_reproducibility(
  original_table = NULL,
  reproduced_table = NULL,
  original_hash = NULL
)
}
\arguments{
\item{original_table}{Optional data frame or table-like object. Defaults to
\code{NULL}. Represents the original randomization result and is required when
\code{original_hash} is \code{NULL}. The object should be the same shape as the
reproduced result so the hash is computed on the full table contents.}

\item{reproduced_table}{Data frame or table-like object to verify. Defaults
to \code{NULL}, but a value is required at call time. It is always hashed and
compared against the original reference.}

\item{original_hash}{Optional character scalar. Defaults to \code{NULL}.
Represents the precomputed SHA-256 hash of the original table. If supplied,
\code{original_table} is not needed. If \code{NULL}, \code{original_table} must be
provided so the hash can be derived.}
}
\value{
A list with four elements:
\describe{
\item{identical}{Logical \code{TRUE} when the hashes match exactly; \code{FALSE}
otherwise.}
\item{original_hash}{Character scalar containing the original SHA-256
hash, either supplied directly or computed from \code{original_table}.}
\item{reproduced_hash}{Character scalar containing the SHA-256 hash of
\code{reproduced_table}.}
\item{message}{Human-readable summary of the comparison result.}
}
}
\description{
This helper compares SHA-256 hashes of two tables and reports whether they
are identical. Use it when you want a strict, hash-based reproducibility
check rather than a semantic or tolerance-based comparison.
}
\details{
The function supports two verification modes: (1) supply \code{original_table}
and \code{reproduced_table}, or (2) supply \code{original_hash} and
\code{reproduced_table}. In both cases, the comparison is performed by exact
SHA-256 hash equality; no semantic equivalence, sorting, rounding, or other
tolerance rules are applied.
}
\examples{
tmp <- tempdir()
set.seed(20260528)
res1 <- rp_randomize(
  project_name = "Demo Trial",
  protocol_no = "P-001",
  sponsor_name = "Demo Sponsor",
  interventions = c(A = "Intervention A", B = "Intervention B"),
  method = "simple",
  seed = 123,
  n_total = 8,
  allocation_ratio = c(A = 1, B = 1),
  generate_report = FALSE,
  generate_reproducibility = FALSE,
  output_dir = tmp
)
set.seed(20260528)
res2 <- rp_randomize(
  project_name = "Demo Trial",
  protocol_no = "P-001",
  sponsor_name = "Demo Sponsor",
  interventions = c(A = "Intervention A", B = "Intervention B"),
  method = "simple",
  seed = 123,
  n_total = 8,
  allocation_ratio = c(A = 1, B = 1),
  generate_report = FALSE,
  generate_reproducibility = FALSE,
  output_dir = tmp
)
rp_verify_reproducibility(res1$allocation_table, res2$allocation_table)

}
\seealso{
\code{\link[=rp_randomize]{rp_randomize()}}
}
