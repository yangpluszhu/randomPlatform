% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rp_launch_app.R
\name{rp_launch_app}
\alias{rp_launch_app}
\title{Launch the local randomPlatform Shiny application.}
\usage{
rp_launch_app(
  host = "127.0.0.1",
  port = 3838,
  launch.browser = TRUE,
  stop_on_session_end = TRUE,
  options = list()
)
}
\arguments{
\item{host}{Character scalar giving the interface to bind to. The default is
\code{"127.0.0.1"}, which keeps the app local to the current machine. Use
\code{"0.0.0.0"} only when you intentionally want broader network reachability;
doing so triggers a warning. Other valid host values are those accepted by
\code{shiny::runApp()}.}

\item{port}{Integer scalar giving the TCP port to listen on. The default is
\code{3838}. Choose a free port if \code{3838} is already in use.}

\item{launch.browser}{Logical scalar. When \code{TRUE} (the default), Shiny will
attempt to open the app in the system browser after launch. Set to \code{FALSE}
when launching in an environment where browser automation is not desired.}

\item{stop_on_session_end}{Logical scalar. When \code{TRUE} (the default), the app
server is wrapped so that \code{shiny::stopApp()} is called when the session
ends. When \code{FALSE}, \code{rp_app_server} is used directly and the app keeps the
standard Shiny session lifecycle.}

\item{options}{Named list of additional arguments passed through to
\code{shiny::runApp()} via \code{do.call()}. Defaults to \code{list()}. Use this to
supply any extra runApp options that are not exposed as formal arguments
here.}
}
\value{
The value returned by \code{shiny::runApp()}, invisibly or visibly as
provided by Shiny.
}
\description{
Starts the bundled Shiny app by building \code{shiny::shinyApp(ui = rp_app_ui(), server = server)} and passing it to \code{shiny::runApp()}.
}
\details{
The app binds to \verb{127.0.0.1} by default so it is only reachable from the
local machine. Binding to \verb{0.0.0.0} exposes the app on all network
interfaces, so the function warns when that host is selected. If
\code{stop_on_session_end = TRUE}, a small wrapper server calls \code{shiny::stopApp()}
when the session ends; otherwise, \code{rp_app_server} is passed through as-is.
Any entries supplied in \code{options} are forwarded directly to
\code{shiny::runApp()}.
}
\examples{
if (interactive()) {
  rp_launch_app()
}

}
\seealso{
\code{\link[=rp_randomize]{rp_randomize()}}
}
