% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/minimization.R
\name{rp_assign_next}
\alias{rp_assign_next}
\title{Assign the next participant in a minimization session.}
\usage{
rp_assign_next(
  session,
  subject_id,
  covariates,
  operator = NA_character_,
  note = NA_character_
)
}
\arguments{
\item{session}{Either an \code{rp_minimization_session} object returned by
\code{rp_minimization_session()} or a single character path to a saved session
RDS file. If a path is supplied, the session is loaded from disk before the
assignment is computed.}

\item{subject_id}{Character scalar identifying the participant. Required;
must be unique among existing \code{session$assignments$subject_id} values.}

\item{covariates}{Named list, named data frame row, or similar named object
containing the current participant's minimization factor values. All
factors listed in \code{session$factors} must be present, and the values are
reordered to \code{session$factors} before scoring. Any extra names are ignored.}

\item{operator}{Optional character scalar identifying the operator entering
the assignment. Default: \code{NA_character_}. Stored in the unblinded row and
written to the session assignment history.}

\item{note}{Optional character scalar with a free-text note. Default:
\code{NA_character_}. Stored in the unblinded row and written to the session
assignment history.}
}
\value{
A list with the following members:
\describe{
\item{session}{Updated \code{rp_minimization_session} object after appending
the new assignment and refreshing the saved RNG state.}
\item{blinded}{A data frame row containing the blinded assignment view,
including \code{subject_id}, \code{envelope_no}, \code{random_no}, \code{drug_code},
\code{blind_label}, and the factor columns.}
\item{unblinded}{The full assignment row with group and intervention
details, timestamps, operator, note, and factor values.}
\item{scores}{Named numeric vector of minimization scores, one per
intervention group, computed before the new assignment is sampled.}
\item{probabilities}{Named numeric vector of sampling probabilities used
to choose the assigned group.}
}
}
\description{
Assign the next participant in a minimization session.
}
\details{
\code{rp_assign_next()} accepts either an in-memory session object or a
path to a saved session RDS file. It validates that \code{subject_id} has not
already been assigned, requires covariates for every factor in
\code{session$factors}, and reorders those covariates to the stored factor order
before scoring. The function restores the saved RNG state, computes the
minimization scores, samples the next group, appends the assignment to the
session, saves the updated session back to \code{session$state_file}, writes
\code{minimization_assignments.csv} in \code{session$output_dir}, and appends an audit
log entry. The returned \code{blinded} and \code{unblinded} rows expose the same new
assignment in blinded and full-detail forms. The \code{generate_random_envelope}
and \code{generate_emergency_envelope} session fields are preserved on the saved
session object but are not otherwise consumed by this function.
}
\examples{
tmp <- file.path(tempdir(), "rp-min-assign")
dir.create(tmp, recursive = TRUE, showWarnings = FALSE)
session <- rp_minimization_session(
  project_name = "Demo study",
  protocol_no = "RP-001",
  sponsor_name = "Acme Pharma",
  interventions = c(Control = "Control", Treatment = "Treatment"),
  factors = c("center", "sex"),
  weights = c(center = 1, sex = 1),
  seed = 123,
  output_dir = tmp
)
result <- rp_assign_next(
  session = session,
  subject_id = "SUBJ-001",
  covariates = list(center = "A", sex = "F")
)
names(result)
file.exists(file.path(tmp, "minimization_assignments.csv"))
}
\seealso{
\code{\link[=rp_minimization_session]{rp_minimization_session()}} for creating the saved session object
used here, and \code{\link[=rp_randomize]{rp_randomize()}} for the batch randomization API that shares
the same intervention normalization rules.
}
