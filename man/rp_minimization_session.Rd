% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/minimization.R
\name{rp_minimization_session}
\alias{rp_minimization_session}
\title{Initialize and save a minimization session.}
\usage{
rp_minimization_session(
  project_name,
  protocol_no,
  sponsor_name,
  interventions,
  allocation_ratio = NULL,
  factors,
  weights = NULL,
  prob_best = 0.8,
  seed,
  state_file = "minimization_session.rds",
  output_dir = ".",
  generate_random_envelope = FALSE,
  generate_emergency_envelope = FALSE
)
}
\arguments{
\item{project_name}{Character scalar naming the project. Required; stored on
the returned session as metadata.}

\item{protocol_no}{Character scalar with the protocol number. Required;
stored on the returned session as metadata.}

\item{sponsor_name}{Character scalar naming the sponsor. Required; stored on
the returned session as metadata.}

\item{interventions}{Intervention definitions in the same normalized forms
accepted by \code{rp_randomize()}: either a named character vector, where names
are \code{group_id} values and values are \code{intervention_name} values, or a data
frame containing \code{group_id} and \code{intervention_name} and optionally
\code{blind_label} and \code{allocation_label}. The number of groups must be between
2 and 5, \code{group_id} values must be unique, and \code{group_id} and
\code{intervention_name} values must not be empty.}

\item{allocation_ratio}{Optional named numeric vector of relative allocation
weights by \code{group_id}. When \code{NULL}, equal weights are used across all
intervention groups. If supplied, names must be unique and match
\code{interventions$group_id} exactly; the values are reordered to that group
order and must be positive because they are used as sampling weights.}

\item{factors}{Character vector that must resolve to 1 to 6 unique
minimization factors after coercion to character and duplicate removal with
\code{unique()}. The resulting factor names are stored in this order, and the
same names must be present in the \code{covariates} supplied to
\code{rp_assign_next()}.}

\item{weights}{Optional numeric vector of factor weights. When \code{NULL}, each
factor gets weight 1. Unnamed vectors must have the same length as
\code{factors} and are taken positionally. Named vectors are matched by factor
name, reordered to the stored factor order, must cover exactly the resolved
\code{factors}, and must use unique names. All weights must be positive.}

\item{prob_best}{Numeric scalar in \verb{(0, 1]} controlling how much probability
mass is given to the best-scoring groups during assignment. Lower values
spread more probability to non-best groups; \code{1} always chooses among the
best-scoring groups only.}

\item{seed}{Integer seed used to initialize the RNG for the session.
Required; the resulting \code{.Random.seed} is stored in the session and reused
by \code{rp_assign_next()}.}

\item{state_file}{File name or relative path for the saved session RDS file.
Default: \code{"minimization_session.rds"}. The path is resolved under
\code{output_dir} and the session is written there immediately.}

\item{output_dir}{Output directory for the session file, assignment CSV, and
audit log. Default: \code{"."}. The directory is created if needed and must be
writable.}

\item{generate_random_envelope}{Logical scalar. Default: \code{FALSE}. Stored on
the session object for downstream consumers; it does not change the
minimization algorithm in this function.}

\item{generate_emergency_envelope}{Logical scalar. Default: \code{FALSE}. Stored
on the session object for downstream consumers; it does not change the
minimization algorithm in this function.}
}
\value{
An object of class \code{rp_minimization_session}, returned invisibly and
saved immediately to \code{state_file}. It is a state container with these
fields:
\describe{
\item{project_name}{Project name metadata.}
\item{protocol_no}{Protocol number metadata.}
\item{sponsor_name}{Sponsor name metadata.}
\item{interventions}{Normalized intervention table used for assignment.}
\item{allocation_ratio}{Relative allocation weights aligned to
\code{interventions$group_id}.}
\item{factors}{Character vector of minimization factor names.}
\item{weights}{Positive factor weights aligned to \code{factors}.}
\item{prob_best}{Best-group sampling probability used by
\code{rp_assign_next()}.}
\item{seed}{Seed used to initialize the RNG.}
\item{state_file}{Normalized path to the session RDS file.}
\item{output_dir}{Validated output directory.}
\item{generate_random_envelope}{Stored logical flag for downstream use.}
\item{generate_emergency_envelope}{Stored logical flag for downstream use.}
\item{assignments}{Data frame of assignments made so far. Initially
empty.}
\item{rng_state}{Saved \code{.Random.seed} value for reproducible
continuation.}
\item{voided}{Character vector of voided assignment identifiers.}
}
}
\description{
Initialize and save a minimization session.
}
\details{
\code{rp_minimization_session()} initializes a stateful minimization
session, normalizes and stores the trial metadata, and saves the session to
the RDS file named by \code{state_file} under \code{output_dir}. The \code{interventions}
argument uses the same normalization path as \code{rp_randomize()}. The saved
object is intended to be passed to \code{rp_assign_next()} either directly or by
file path so that the assignment sequence can resume from the stored RNG
state.
}
\examples{
tmp <- file.path(tempdir(), "rp-min-session")
dir.create(tmp, recursive = TRUE, showWarnings = FALSE)
session <- rp_minimization_session(
  project_name = "Demo study",
  protocol_no = "RP-001",
  sponsor_name = "Acme Pharma",
  interventions = c(Control = "Control", Treatment = "Treatment"),
  factors = c("center", "sex"),
  weights = c(center = 1, sex = 1),
  seed = 123,
  output_dir = tmp
)
session$state_file
}
\seealso{
\code{\link[=rp_assign_next]{rp_assign_next()}} for assigning participants from a saved
session, and \code{\link[=rp_randomize]{rp_randomize()}} for batch randomization workflows that share
the same intervention normalization rules.
}
